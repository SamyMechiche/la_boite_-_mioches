<form method="post" action="{{ path('app_child_crud_delete', {'id': child.id}) }}" onsubmit="return confirm('Are you sure you want to delete this child?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ child.id) }}">
    <button class="crud-button danger">Delete</button>
</form>
